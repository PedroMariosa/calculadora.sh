##codigo da calculadora 

operação = input('escolha um comando entre (+, -, *, /):')
num1 = float(input('digite o primeiro número: '))
num2 = float(input('digite o segundo número: '))

if operação == '+':
  resultado = num1 + num2
elif operação == '-':
  resultado = num1 - num2
elif operação == '*':
   resultado = num1 * num2
elif operação == '/':
  resultado = num1 / num2
else:
  resultado = "operação invalida"

print(float(resultado)
